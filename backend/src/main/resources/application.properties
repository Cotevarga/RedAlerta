spring.application.name=demo

# ==========================================
# CONEXIÓN A POSTGRESQL (Neon.tech)
# ==========================================
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-red-grass-ac1rm1qb.sa-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_jh3BcXCZO8MV}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
# HIBERNATE / JPA
# ==========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==========================================
# JWT
# ==========================================
jwt.secret=${JWT_SECRET:}
jwt.expiration=${JWT_EXPIRATION:36000000}

# ==========================================
# CREDENCIALES DE ADMIN
# ==========================================
admin.username=${ADMIN_USERNAME:adminMuni}
admin.password=${ADMIN_PASSWORD:Corral2026}

# ==========================================
# CORS
# ==========================================
app.cors.origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:5174}